load("//kernel:templates.bzl", "kernel_binary")

kernel_binary(
    name = "kernel",
    visibility = ["//visibility:public"],
    srcs = [
        "kernel.cc",
    ],
    deps = [
        "//kernel/arch:memory",
        "//kernel/arch:serial",
        "//libc/stdio",
    ],
)
