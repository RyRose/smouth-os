load("//kernel:templates.bzl", "kernel_binary")

kernel_binary(
    name = "kernel",
    srcs = [
        "kernel.cc",
    ],
    visibility = ["//visibility:public"],
    deps = [
        "//kernel/arch:init",
        "//kernel/arch:memory",
        "//kernel/arch:serial",
        "//libc:stdio",
    ],
)
